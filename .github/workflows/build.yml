name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master # check out code
    - run: sudo apt-get update && sudo apt-get install vala
    - run: meson build
    - run: ninja -C build
