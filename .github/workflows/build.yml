name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@master # check out code
    - run: sudo pip3 install meson
    - run: sudo apt-get update && sudo apt-get install valac ninja
    - run: meson build
    - run: ninja -C build
